export const AUTH_END_POINT = "https://accounts.spotify.com/authorize"

const REDIRECT_URL = "http://http://localhost:5173/"

const CLIENT_ID = import.meta.env.VITE_CLIENT_ID

const SCOPES = [
    "user-read-private"
]

export const LOGIN_URL = ''